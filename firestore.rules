rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /leads_ebook_airfryer/{document=**} {
      allow read, write: if true; // Temporary open rules for direct capture
    }
  }
}